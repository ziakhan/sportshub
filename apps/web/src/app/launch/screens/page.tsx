/**
 * Public mirror of the /dev/home-shots contact sheet (owner 2026-08-17:
 * "send it to my dev to examine" — /dev/* bounces signed-out visitors in
 * production, so the shareable copy lives under the public /launch prefix).
 */
export { default } from "../../dev/home-shots/page"
export const dynamic = "force-dynamic"
