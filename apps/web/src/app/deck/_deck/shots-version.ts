/**
 * Bumped by scripts/marketing/deck-shots-convert.py on every re-shoot.
 *
 * The screenshot filenames never change, so without this a browser that has
 * seen the deck keeps serving the old pictures out of memory cache and a
 * re-shoot looks like it did nothing. Appended to every shot URL.
 *
 * Do not edit by hand.
 */
export const SHOTS_VERSION = "20260820-2019"
